using NUnit.Framework;
using Newtonsoft.Json;
using Dal.Helpers;
using Dal.Models;
using Moq;

namespace Tests
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        //[Test]
        //public void Test1()
        //{
        //    var someUser = new Mock<User>();
        //    var jsonExpected = JsonConvert.SerializeObject(someUser.Object);
            //act
        //    var jsonActual = JsonHelper.Serializer<User>(someUser.Object);

        //    Assert.Pass();
        //}
    }
}